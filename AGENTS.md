# AGENTS.md

## 프로젝트 개요

이 저장소는 NEIS 공개 API를 활용해 급식 정보와 학교 정보를 조회하는 웹 애플리케이션을 구축하는 워크숍용 저장소입니다. 목표는 엑셀 기반의 NEIS API 명세를 OpenAPI 3.0 사양으로 변환하고, 이후 앱 개발, 테스트, 배포, MCP 서버 구현까지 진행하는 것입니다.

## 핵심 원칙

- 프로젝트는 이슈 중심 워크플로우를 따라야 합니다.
- 각 변경은 관련 이슈와 연결되도록 설명해야 합니다.
- 민감한 값(예: API 키, 인증 정보, 시크릿)은 코드나 커밋에 포함하지 않습니다.
- 변경 사항은 문서와 코드가 서로 일치하도록 유지합니다.
- 작업을 완료하면 반드시 문서화와 검증을 수행합니다.

## 저장소 구조

- `data/`: NEIS 공개 API 명세 엑셀 파일 및 생성된 `openapi.json`
- `docs/`: 단계별 워크숍 가이드 문서
- `.github/`: GitHub 설정, 이슈 템플릿, PR 템플릿
- `README.md`: 워크숍 개요

## 작업 규칙

### 1. OpenAPI 문서 관련 작업

- OpenAPI 문서 생성 시 기본 규격은 OpenAPI 3.0.4를 준수합니다.
- `data/openapi.json` 파일은 유효한 JSON 형식이어야 합니다.
- `paths`에는 모든 API가 HTTP 메서드와 경로별로 빠짐없이 포함되어야 합니다.
- 각 요청 파라미터는 이름, 위치(`query`, `path`, `header`), 필수 여부, 데이터 타입, 설명을 포함해야 합니다.
- 성공/오류 응답은 상태 코드, 설명, 콘텐츠 타입, 응답 스키마를 정의해야 합니다.
- 변경이 발생하면 반드시 OpenAPI validator 또는 동등한 검증 절차로 확인합니다.

### 2. 코드 및 문서 변경

- 기존 구조와 일관된 디렉터리 구조를 유지합니다.
- 문서 수정이 필요한 경우 관련 `docs/*.md` 파일을 함께 업데이트합니다.
- 코드를 추가하거나 수정할 때는 기능 목적이 명확하게 설명되도록 작성합니다.

### 3. PR 작성 규칙

- 모든 PR은 `.github/PULL_REQUEST_TEMPLATE.md` 템플릿을 반드시 사용합니다.
- PR 본문에는 Description, Related Issue, Type of Change, Validation, Checklist 항목을 포함해야 합니다.
- `Related Issue`에는 관련 이슈 번호를 반드시 기입합니다.
- 시크릿, 인증 키, 사용자별 로컬 설정 값은 포함하지 않습니다.

## 검증 기준

- JSON 문법이 정상인지 확인합니다.
- OpenAPI 사양 준수 여부를 확인합니다.
- 생성한 문서가 실제 데이터 소스(엑셀 명세서)와 일치하는지 확인합니다.
- 필요한 경우 로컬 실행 또는 스키마 검증 명령을 사용해 확인합니다.

## 권장 작업 흐름

1. 관련 이슈를 확인합니다.
2. 필요한 파일과 문서를 읽습니다.
3. 최소 변경으로 구현합니다.
4. 검증을 수행합니다.
5. PR 템플릿에 맞춰 변경 내용을 정리합니다.

## 참고 파일

- `.github/PULL_REQUEST_TEMPLATE.md`
- `docs/01-generate-openapi.md`
- `docs/02-generate-agents-md.md`
- `README.md`
